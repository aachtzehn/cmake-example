# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool: Default

steps:
- task: CMake@1
  inputs:
    cmakeArgs: -H. -Bbuild -DCMAKE_INSTALL_PREFIX:PATH=/tmp/foo